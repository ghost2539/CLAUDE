fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
sqlalchemy>=2.0,<3
psycopg[binary]>=3.1,<4
# A produção conecta com postgresql+psycopg2://; o driver 2 precisa existir também.
psycopg2-binary>=2.9
# Driver MySQL (servidor novo). PyMySQL é puro-Python (fácil de instalar);
# 'cryptography' é exigido pelo auth caching_sha2_password do MySQL 8.
PyMySQL>=1.1,<2
cryptography>=42
python-multipart>=0.0.9
requests>=2.31
# Driver da base do EBS (integracoes/ebs_oracle.py). Modo thin por padrão;
# o Instant Client só é necessário onde o banco exigir modo thick.
oracledb>=2.0
pandas>=2.2
openpyxl>=3.1
itsdangerous>=2.2
pydantic>=2.7,<3
slowapi>=0.1.9
