fastapi>=0.115,<1
uvicorn[standard]>=0.30,<1
sqlalchemy>=2.0,<3
psycopg[binary]>=3.1,<4
# Driver MySQL (servidor novo). PyMySQL é puro-Python (fácil de instalar);
# 'cryptography' é exigido pelo auth caching_sha2_password do MySQL 8.
PyMySQL>=1.1,<2
cryptography>=42
# Driver Oracle (EBS) — usado só pelo módulo ebs_oracle.py (leitura na base do EBS).
# Modo thick usa o Instant Client (ORACLE_CLIENT_LIB_DIR); sem ele cai para thin.
oracledb>=2.0,<4
python-multipart>=0.0.9
requests>=2.31
pandas>=2.2
openpyxl>=3.1
itsdangerous>=2.2
pydantic>=2.7,<3
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
slowapi>=0.1.9
# Leitura de PDF (extração de campos da NF em Agendamentos Forn.)
pypdf>=4.0
# mTLS com o certificado A1 (.pfx) na consulta da NF-e à SEFAZ (integracoes/nfe_sefaz.py); exige cryptography>=42.
requests-pkcs12>=1.25
# DANFE em PDF a partir do XML da NF-e (integracoes/nfe_sefaz.py::danfe_pdf).
brazilfiscalreport>=1.0
